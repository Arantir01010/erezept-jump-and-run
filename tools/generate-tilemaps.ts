/**
 * ERSETZT durch den Level-Baukasten.
 *
 * Die Prototyp-Level, die dieses Skript früher hart verdrahtet erzeugte,
 * leben jetzt als editierbare Quellen in levels-src/<id>.level.json
 * (ASCII-Karten + Metadaten). Kompilieren:
 *
 *     npm run levels
 *
 * Anleitung für Level-Autoren (Mensch wie KI): levels-src/ANLEITUNG.md
 */
console.error('Dieses Skript ist Geschichte — Level werden aus levels-src/ gebaut: npm run levels')
process.exit(1)
